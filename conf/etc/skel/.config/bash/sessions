#!/bin/env bash

function raviza_systems() {

  local system="root/.config/bash/systems"
  test -f $system/basher && source $system/bashir
  test -f $system/aliase && source $system/aliase
}


function raviza_loader() {
  raviza_systems
}

raviza_loader
